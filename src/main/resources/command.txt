docker exec -it kafka bash

kafka-topics.sh --create \
  --topic order.events \
  --bootstrap-server localhost:9092 \
  --partitions 1 \
  --replication-factor 1

find / -name kafka-topics.sh 2>/dev/null
/opt/kafka/bin/kafka-topics.sh --create \
  --topic order.events \
  --bootstrap-server localhost:9092 \
  --partitions 1 \
  --replication-factor 1


kafka-topics --list --bootstrap-server kafka:9092
/opt/kafka/bin/kafka-topics.sh   --bootstrap-server localhost:9092   --list

/opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server localhost:9092 \
  --describe \
  --topic order.events

kafka-console-consumer \
  --topic order.events \
  --bootstrap-server kafka:9092 \
  --from-beginning

  /opt/kafka/bin/kafka-console-consumer.sh   --bootstrap-server localhost:9092   --topic order.events   --
  from-beginning


 /opt/kafka/bin/kafka-topics.sh --delete --topic order.events --bootstrap-server localhost:9092